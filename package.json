{
  "name": "frontend",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@10.15.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm -F web dev",
    "build": "pnpm -r build",
    "clean": "rm -rf node_modules pnpm-lock.yaml **/node_modules **/dist"
  }
}
