/**
 * scrollPresentation.ts — Scrollbar premium Nexia (cyan eléctrico).
 *
 * - CSS global: `body` en index.css
 * - Contenedores: `overflow-y-auto` + `NEXIA_SCROLLBAR`
 * - Shells móvil: `h-[100dvh] overflow-hidden` en wrapper + scroll en `<main>`
 */

/** Scrollbar vertical/horizontal premium — track transparente, thumb cyan. */
export const NEXIA_SCROLLBAR = "scrollbar-nexia";
